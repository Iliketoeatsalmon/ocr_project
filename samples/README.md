# Samples Directory

- `raw/`: Place original test images captured from the Basler camera here.
- `debug/`: Store intermediate debug outputs here (cropped ROIs, threshold images, overlays).

Do not commit sensitive or production images unless explicitly approved.
